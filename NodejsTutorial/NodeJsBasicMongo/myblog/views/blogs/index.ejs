<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <title>Blogs</title>
</head>

<body>

    <%- include('../layout') %>
        <div class="py-2">
            <a href="/blogs/add" class="btn btn-primary">เขียนบทความ</a>
        </div>
        <hr>

        <h2>ข้อมูลบทความทั้งหมด</h2>
        <div class="container">
            <% Object.values(blogs).forEach(function(blog) { %>
                <h4> <b>บทความ</b>
                    <%= blog.title %>
                </h4>
                <p> <b>ผู้เขียน</b>
                    <%= blog.author %>
                </p>
                <p> <b>หมวด</b>
                    <%= blog.category %>
                </p>

                <a href="/blogs/<%= blog._id %>">แก้ไข</a>
                <a href="/blogs/delete/<%= blog._id %>" onclick="return confirm('ต้องการลบหรือไม่')">ลบ</a>
                <hr>
            <% }) %>
        </div>


        <p>by Thongchai</p>
</body>

</html>